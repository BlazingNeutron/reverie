name: Build Docker Images

on:
  workflow_run:
    workflows: [CI]
    types: [completed]
    branches: [main]

permissions:
  contents: read
  packages: write

jobs:
  build_images:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    name: Build and push images
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build migrator image
        uses: docker/build-push-action@v7
        with:
          context: ./docker/migrator/
          file: ./docker/migrator/Dockerfile
          build-contexts: |
            migrations=./supabase/migrations
          tags: ghcr.io/blazingneutron/reverie-migrator:${{ github.sha }},ghcr.io/blazingneutron/reverie-migrator:latest
          push: true
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}:latest
          cache-to: type=inline
      - name: Build reverie-web image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/web/Dockerfile
          tags: ghcr.io/blazingneutron/reverie-web:${{ github.sha }},ghcr.io/blazingneutron/reverie-web:latest
          push: true
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}:latest
          cache-to: type=inline
      - name: Build api image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./apps/api/Dockerfile
          tags: ghcr.io/blazingneutron/reverie-api:${{ github.sha }},ghcr.io/blazingneutron/reverie-api:latest
          push: true
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}:latest
          cache-to: type=inline
