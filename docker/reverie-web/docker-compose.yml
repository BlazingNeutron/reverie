services:
  reverie-web:
    image: ghcr.io/blazingneutron/reverie-web
    restart: always
    build:
      context: ../../apps/web
      dockerfile: Dockerfile
      additional_contexts:
        root: ../../
    env_file:
      - ../.env
